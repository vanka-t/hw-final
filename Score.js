class Score{
    constructor(){
this.x = 0;
this.y = 0;
this.r = 20;
    }

    show(){
        // scoreSprite = createSprite(this.x+300,hh-this.y, this.length,this.thick);
        // scoreSprite.addImage(barIcon);
        // drawSprite(barSprite);
    }
}